/*
in a scalable enterprise application
this would typically be in a directory structure
dedicated to interfaces
or composed of model classes/constructors instead
*/
export class Hero {
  id: number;
  name: string;
}
